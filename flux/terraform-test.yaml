apiVersion: infra.contrib.fluxcd.io/v1alpha2
kind: Terraform
metadata:
  name: test
  namespace: flux-system
spec:
  interval: 1m
  approvePlan: auto
  path: ./flux/terraform/test
  sourceRef:
    kind: GitRepository
    name: k3d-recipes
    namespace: flux-system
